{
  "name": "@demo/cli-serve",
  "version": "0.0.2",
  "main": "lib/index",
  "license": "MIT",
  "scripts": {
    "test": "echo 2",
    "build": "echo 'serve start' && yarn clean && tsc && echo 'serve end'",
    "clean": "rimraf -rf ./lib",
    "lint": "eslint ./src"
  },
  "dependencies": {
    "@demo/cli-utils": "^0.0.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
