{
  "name": "@demo/cli",
  "version": "0.0.4",
  "main": "lib/index",
  "bin": {
    "test-lerna-cli": "./bin/test-lerna-cli.js"
  },
  "license": "MIT",
  "scripts": {
    "test": "echo 1",
    "build": "echo 'cli start' && yarn clean && tsc && echo 'cli end'",
    "clean": "rimraf -rf ./lib",
    "lint": "eslint ./src"
  },
  "dependencies": {
    "@demo/cli-serve": "^0.0.4",
    "@demo/cli-utils": "^0.0.4"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
